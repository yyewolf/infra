apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  dependsOn:
  - name: cilium
  interval: 10m
  path: ./flux/platform/cert-manager
  prune: false
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
