apiVersion: apps/v1
kind: Deployment
metadata:
  name: error-pages
  labels:
    app: error-pages
spec:
  replicas: 1
  selector:
    matchLabels:
      app: error-pages
  template:
    metadata:
      labels:
        app: error-pages
    spec:
      containers:
        - name: error-pages
          image: tarampampam/error-pages@sha256:96855caa28e08a96bb9ff746a7c094b43235351bad79806c2210f9e7677abce9
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: TEMPLATE_NAME 
              value: "ghost"
---
kind: Service
apiVersion: v1
metadata:
  name: errors
spec:
  type: ClusterIP
  selector:
    app: error-pages
  ports:
    - protocol: TCP
      port: 80
      targetPort: http