apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: postgres

resources:
  - namespace.yaml
  - backup-credentials-sops.yaml
  - objectstore.yaml
  - cluster.yaml
  - scheduledbackup.yaml
  - network-policy.yaml
