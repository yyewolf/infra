apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vaultwarden
  namespace: postgres
spec:
  provider:
    kubernetes:
      remoteNamespace: vaultwarden
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-push-writer
