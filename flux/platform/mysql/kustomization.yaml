apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mysql

resources:
  - namespace.yaml
  - backup-credentials-sops.yaml
  - cluster.yaml
  - scheduledbackup.yaml
  - network-policy.yaml
