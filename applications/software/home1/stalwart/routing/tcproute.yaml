apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: smtp
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: mail-smtp
    sectionName: smtp
  rules:
    - backendRefs:
      - group: ""
        kind: Service
        name: stalwart-smtp
        port: 25
        weight: 1
