apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - runtimeclass.yaml
  # compat-sysbox-runc.yaml used to live here, aliasing gVisor. It now aliases
  # the real sysbox runtime and moved to ../sysbox/.
