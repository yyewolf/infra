apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: umami-db
  namespace: postgres
spec:
  refreshInterval: 1h
  deletionPolicy: None
  secretStoreRefs:
  - name: umami
    kind: SecretStore
  selector:
    secret:
      name: umami-db-credentials
  template:
    engineVersion: v2
    data:
      uri: postgresql://{{ .username | urlquery }}:{{ .password | urlquery }}@postgres-rw.postgres.svc.cluster.local:5432/umami
  data:
  - match:
      secretKey: uri
      remoteRef:
        remoteKey: umami-db
        property: DATABASE_URL
