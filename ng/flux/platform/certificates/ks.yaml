apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificates
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  dependsOn:
  - name: cert-manager
  interval: 10m
  path: ./ng/flux/platform/certificates
  prune: false
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
