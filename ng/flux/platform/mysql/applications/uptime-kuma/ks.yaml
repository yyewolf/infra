apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: uptime-kuma-db
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  dependsOn:
  - name: external-secrets
  - name: mysql
  interval: 10m
  path: ./ng/flux/platform/mysql/applications/uptime-kuma
  prune: false
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
