apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
    spec:
      nodeSelector:
        location: home1
      tolerations:
      - key: "PrivateNode"
        operator: "Equal"
        value: "home1"
      containers:
      - name: minio
        image: quay.io/minio/minio:latest@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
        args:
        - server
        - /data
        - --console-address
        - ":9001"
        ports:
        - containerPort: 9000
        env:
        - name: MINIO_ROOT_USER
          valueFrom:
            secretKeyRef:
              name: minio-secret
              key: root_user
        - name: MINIO_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: minio-secret
              key: root_password
        volumeMounts:
        - name: data
          mountPath: /data
      - name: minio-console
        image: ghcr.io/astrateam-net/minio-browser:1.7.6@sha256:7b488dee696792f17231bfc36684ea92c84eb7f2021653d026f6c9949cff7b78
        command:
        - ./console
        - server
        - --port
        - "9002"
        env:
        - name: CONSOLE_MINIO_SERVER
          value: http://localhost:9000
        ports:
        - containerPort: 9002
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: minio-storage
