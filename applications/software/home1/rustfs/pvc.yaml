apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-rustfs-0
  namespace: rustfs
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path-home1-retain
