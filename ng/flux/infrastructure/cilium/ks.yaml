apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cilium
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 10m
  path: ./ng/flux/infrastructure/cilium
  prune: true
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
