apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
      annotations:
        k8up.io/backupcommand: sh -c 'pg_dumpall --user=postgres --clean'
        k8up.io/file-extension: .sql
    spec:
      containers:
        - name: postgres
          image: ghcr.io/tensorchord/pgvecto-rs:pg17-v0.4.0@sha256:0e7abbc5fe9252d05d99492ccae04ac27b1b2a1a8297d51e9ddba27fbd52b2c3
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password
          volumeMounts:
            - name: postgres-storage
              subPath: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 0m
              memory: 0Mi
            limits:
              cpu: 500m
              memory: 400Mi
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
