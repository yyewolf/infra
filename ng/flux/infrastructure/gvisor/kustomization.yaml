apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - runtimeclass.yaml
  # Temporary. See the file — it is a migration bridge, not part of the design.
  - compat-sysbox-runc.yaml
