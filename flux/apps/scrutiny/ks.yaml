apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: scrutiny
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  dependsOn:
  - name: tailscale
  - name: longhorn
  interval: 10m
  path: ./flux/apps/scrutiny
  prune: false
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
