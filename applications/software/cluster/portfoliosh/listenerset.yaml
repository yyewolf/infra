apiVersion: gateway.networking.x-k8s.io/v1alpha1
kind: XListenerSet
metadata:
  name: ssh
spec:
  parentRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    namespace: kgateway
    name: kgateway
  listeners:
  - name: ssh
    protocol: TCP
    port: 22
