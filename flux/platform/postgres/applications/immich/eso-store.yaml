apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: immich
  namespace: postgres
spec:
  provider:
    kubernetes:
      remoteNamespace: album
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-push-writer
